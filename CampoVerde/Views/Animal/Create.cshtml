@model CampoVerde.Models.Animal

@{
    ViewData["Title"] = "Nuevo Animal - Campo Verde";
}

<style>
    body {
        background-color: #eaf2e9;
        font-family: 'Segoe UI', sans-serif;
    }

    .card-ganaderia {
        background-color: #fdfdf5;
        border-radius: 30px;
        box-shadow: 0 10px 30px rgba(74, 124, 74, 0.1);
        padding: 45px;
        border: 1px solid #d4e0d4;
    }

    h2 {
        font-family: 'Georgia', serif;
        color: #1a3c1a;
        font-weight: 700;
        margin-bottom: 35px;
    }

    .form-label {
        color: #3d663d;
        font-weight: 600;
    }

    .form-control, .form-select {
        background-color: #f5f9f5 !important;
        border: 1px solid #c8d9c8;
        border-radius: 15px;
        padding: 12px 18px;
        color: #2d3a2d;
    }

        .form-control:focus {
            border-color: #4a7c4a;
            box-shadow: 0 0 0 0.2rem rgba(74, 124, 74, 0.25);
        }

    .btn-crear {
        background: linear-gradient(135deg, #4a7c4a, #346334);
        color: white;
        border-radius: 50px;
        padding: 12px 45px;
        font-weight: bold;
        border: none;
        transition: all 0.4s ease;
    }

        .btn-crear:hover {
            background: #2d4d2d;
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(61, 102, 61, 0.3);
            color: white;
        }

    .btn-cancelar {
        background-color: #d1e0d1;
        color: #2d4d2d;
        border-radius: 50px;
        padding: 12px 35px;
        border: none;
        text-decoration: none;
    }
</style>

<div class="container mt-5 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-9 col-lg-8">
            <div class="card card-ganaderia">
                <h2>Registrar nuevo ejemplar</h2>

                <form asp-action="Create" method="post" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label asp-for="codigo" class="form-label">Código del Animal</label>
                            <input asp-for="codigo" class="form-control" placeholder="Ej. CV-001" />
                            <span asp-validation-for="codigo" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label asp-for="nombre" class="form-label">Nombre/Identificador</label>
                            <input asp-for="nombre" class="form-control" placeholder="Ej. Luna" />
                            <span asp-validation-for="nombre" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label asp-for="raza" class="form-label">Raza</label>
                            <input asp-for="raza" class="form-control" placeholder="Ej. Brahman" />
                            <span asp-validation-for="raza" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label asp-for="Estado" class="form-label">Estado Actual</label>
                            <select asp-for="Estado" class="form-select">
                                <option value="" disabled selected>Seleccione el estado...</option>
                                @foreach (var e in Enum.GetValues(typeof(EstadoAnimal)))
                                {
                                    <option value="@e">@e</option>
                                }
                            </select>
                            <span asp-validation-for="Estado" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label asp-for="lote" class="form-label">Lote / Potrero</label>
                            <input asp-for="lote" class="form-control" placeholder="Ej. Lote Norte" />
                            <span asp-validation-for="lote" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label asp-for="pesoActual" class="form-label">Peso Actual (kg)</label>
                            <input asp-for="pesoActual" type="number" step="0.1" class="form-control" placeholder="0.0" />
                            <span asp-validation-for="pesoActual" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12 mb-4">
                            <label asp-for="fechaNacimiento" class="form-label">Fecha de Nacimiento</label>
                            <input asp-for="fechaNacimiento" type="date" class="form-control" />
                            <span asp-validation-for="fechaNacimiento" class="text-danger small"></span>
                        </div>
                        <div class="col-md-12 mb-4">
                            <label class="form-label">Fotografía del Animal</label>
                            <input type="file" name="imagenArchivo" class="form-control" accept="image/*" />
                        </div>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mt-4">
                        <a asp-action="Index" class="btn btn-cancelar">Cancelar</a>
                        <button type="submit" class="btn btn-crear">Guardar Registro</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}